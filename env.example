# --- Sentry (Web: Next.js) ---
# クライアント（ブラウザ）でも使うDSN。公開しても問題ない値です。
NEXT_PUBLIC_SENTRY_DSN=

# サーバ側（Next.js）で使うDSN。NEXT_PUBLIC_SENTRY_DSN と同じ値でもOKです。
SENTRY_DSN=

# 環境名（dev/stg/prod など）
SENTRY_ENVIRONMENT=dev

# パフォーマンス計測（必要なときだけ。0〜1）
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0
SENTRY_TRACES_SAMPLE_RATE=0

# --- Source Maps Upload (build/CI only) ---
# これらは秘密情報（特に SENTRY_AUTH_TOKEN）なのでクライアントに公開しないこと
# Cloudflare Pages では `npm run build:cf` がこれらを使って sourcemaps upload を実行します
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=


