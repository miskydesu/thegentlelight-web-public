# The Gentle Light — Web (公開用・フォーク用)
# このファイルを .env.local にコピーし、必要な値だけ設定してください。
# 本番の API キー・トークン・DB 接続情報は絶対にここに書かないでください。

# -----------------------------------------------------------------------------
# 公開前提（最小限で UI を動かす）
# -----------------------------------------------------------------------------

# バックエンド API のベース URL（本番・ステージングで設定）
# 未設定かつ USE_MOCK_DATA=1 のときは fixtures/*.json で UI が動きます。
# NEXT_PUBLIC_API_BASE_URL=https://api.thegentlelight.org

# サイトの正規 URL（canonical・OG・hreflang 用）。本番では必須。
# NEXT_PUBLIC_SITE_URL=https://thegentlelight.org

# 画像配信のベース URL（カラム表紙・著者画像など）。未設定時は相対パス。
# NEXT_PUBLIC_IMAGE_BASE_URL=

# -----------------------------------------------------------------------------
# オプション（未設定でもビルド・表示は成立）
# -----------------------------------------------------------------------------

# API なしで UI だけ動かすモード（フォーク・デモ・PH 用）
# 1 にすると fixtures/*.json を読み、NEXT_PUBLIC_API_BASE_URL が無くてもビルド・表示可能。
# USE_MOCK_DATA=1

# 検索エンジンにインデックスさせない（ステージング用）
# ROBOTS_NOINDEX=true

# 環境名（Sentry などで利用）
# APP_ENV=production

# -----------------------------------------------------------------------------
# 本番のみ（公開 repo には値を含めない）
# -----------------------------------------------------------------------------

# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google ログイン（OAuth クライアント ID。公開してもよいが、制限設定は本番用に別途）
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Sentry（エラー監視）
# NEXT_PUBLIC_SENTRY_DSN=  または SENTRY_DSN=
# SENTRY_ENVIRONMENT=
# SENTRY_TRACES_SAMPLE_RATE=0.1
# ビルド時ソースマップアップロード用（CI のみ設定）:
# SENTRY_AUTH_TOKEN=   SENTRY_ORG=   SENTRY_PROJECT=

# Cloudflare Pages ビルド時に自動設定されるため、手動設定不要:
# CF_PAGES, CF_PAGES_BRANCH, CF_PAGES_URL
