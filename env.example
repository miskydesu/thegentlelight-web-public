# The Gentle Light — Web (for public use / forking)
# Copy this file to .env.local and set only the values you need.
# Do not put production API keys, tokens, or DB credentials here.

# -----------------------------------------------------------------------------
# Minimum to run the UI
# -----------------------------------------------------------------------------

# Backend API base URL (set in production or staging).
# If unset and USE_MOCK_DATA=1, the UI uses fixtures/*.json.
# NEXT_PUBLIC_API_BASE_URL=https://api.thegentlelight.org

# Canonical site URL (canonical, OG, hreflang). Required in production.
# NEXT_PUBLIC_SITE_URL=https://thegentlelight.org

# Base URL for images (column covers, author images). Omit for relative paths.
# NEXT_PUBLIC_IMAGE_BASE_URL=

# -----------------------------------------------------------------------------
# Optional (build and UI work without these)
# -----------------------------------------------------------------------------

# Run UI without the API (fork / demo). Set to 1 to use fixtures/*.json.
# USE_MOCK_DATA=1

# Prevent search engines from indexing (e.g. staging).
# ROBOTS_NOINDEX=true

# Environment name (e.g. for Sentry).
# APP_ENV=production

# -----------------------------------------------------------------------------
# Production only (do not put real values in the public repo)
# -----------------------------------------------------------------------------

# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Sign-in (OAuth client ID; can be public, restrict in Google Console).
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Sentry (error monitoring)
# NEXT_PUBLIC_SENTRY_DSN= or SENTRY_DSN=
# SENTRY_ENVIRONMENT=
# SENTRY_TRACES_SAMPLE_RATE=0.1
# Build-time sourcemap upload (CI only):
# SENTRY_AUTH_TOKEN=   SENTRY_ORG=   SENTRY_PROJECT=

# Set automatically by Cloudflare Pages; no need to set manually:
# CF_PAGES, CF_PAGES_BRANCH, CF_PAGES_URL
